# --- Required for AI parsing of voice / text thoughts ---
ANTHROPIC_API_KEY=sk-ant-...

# Override the parser model (defaults to claude-sonnet-4-6)
# PARSER_MODEL=claude-sonnet-4-6

# --- Speech-to-text (pick ONE) ---

# Option A: cloud Whisper (recommended; fastest setup)
# OPENAI_API_KEY=sk-...
# WHISPER_MODEL=whisper-1

# Option B: local faster-whisper (fallback if OPENAI_API_KEY is unset)
#   pip install faster-whisper
# LOCAL_WHISPER_MODEL=small

# --- Misc ---
# BRAIN_DB=/absolute/path/to/brain.json

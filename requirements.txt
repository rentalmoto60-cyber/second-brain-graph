networkx>=3.0
pytest>=7.0
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
websockets>=12.0
python-multipart>=0.0.9
anthropic>=0.40
python-dotenv>=1.0
bcrypt>=4.0
# Optional, install one of these for speech-to-text:
#   openai>=1.40             (cloud Whisper, needs OPENAI_API_KEY)
#   faster-whisper>=1.0      (local fallback, ~100MB model on first run)
